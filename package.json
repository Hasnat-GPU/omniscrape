{
  "name": "omniscrape-mcp",
  "version": "0.1.0",
  "description": "Point-and-click web scraper that connects your browser to Claude over MCP.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:server && npm run test:extension",
    "test:server": "npm --prefix mcp-server test",
    "test:extension": "node --test --test-timeout=20000 \"tests/**/*.test.js\"",
    "install:all": "npm install && npm --prefix mcp-server install"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "ws": "^8.19.0"
  }
}
